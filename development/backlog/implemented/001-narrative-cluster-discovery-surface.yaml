schema_version: 1
story:
  id: 1
  title: Narrative-Cluster Discovery Surface
  state: implemented
  created_on: 2026-07-08
  last_updated_on: 2026-07-09
  source_todo:
    - development/todos/001-trial-US-015.md
  actor: human trader
  goal: >
    When reviewing narrative or topic findings drawn from earnings reports, SEC filings, and
    news coverage, the human trader sees tickers grouped into clusters by shared or adjacent
    narrative, independent of whether any pair bond or sync signal exists between them and
    independent of existing theme membership. For each cluster the human trader can see which
    storyline or topic ties it together, and how its frequency of mention or company breadth is
    trending. From a cluster the human trader can move directly into a side-by-side chart
    inspection of its members to assess whether to create a theme from it (STR-011).
  success_criteria:
    - Extract narratives and topics from earnings reports, SEC filings, and news coverage per
      ticker using the FMP data provider, refreshed on a nightly batch cadence.
    - Group tickers into clusters via topic modeling on extracted narratives, based on shared or
      overlapping topics, independent of whether a pair bond or sync signal exists between them,
      and independent of existing theme membership.
    - For each cluster, display a short topic label or summary of the storyline that ties it
      together, and how its frequency of mention or company breadth is trending.
    - Enable the human trader to move directly from a cluster into a side-by-side chart
      inspection of its members.
    - Theme creation from a narrative cluster remains the human trader's discretionary judgment
      made after chart inspection; the Technical Trader Solution must never automatically create
      a theme from narrative clustering alone.
  architecture_options:
    - option: Fixed nightly pipeline (fetch from FMP, extract, topic-model, label, persist),
        classified solution_surface coded_agent, feeding a separate Discovery Surface
        presentation component. Chosen; see docs/architecture/01-technical-trader-solution.md
        Operating Decision Record.
      alternatives_considered:
        - Flexible/exploratory frontier-reasoning step judging narrative connections case by
          case; rejected as too slow and costly to run nightly across the full eligible
          universe, confirmed with the human trader on 2026-07-08.
        - Fixed pipeline now with a flexible reasoning step deferred to a follow-up story;
          available if the fixed pipeline later proves too coarse for a narrow ticker subset.
  implementation_options:
    - option: Use the FMP API to source raw earnings reports, SEC filings, and news for target
        tickers on a nightly batch cadence. Apply topic modeling to derive narrative clusters
        from extracted per-ticker findings. Integrate cluster results into the Technical Trader
        Solution with interactive, side-by-side chart visualization. See
        docs/implementation/01-narrative-cluster-discovery-surface.md for the full data
        contracts, storage, and interface specification.
      alternatives_considered:
        - Custom API-plus-frontend build for the Discovery Surface page; rejected in favor of
          Streamlit for faster delivery of an interactive, Python-native page reusing the
          pipeline's own data-access functions directly.
        - BERTopic chosen as the topic-modeling library over classic LDA and scikit-learn NMF
          (weaker embedding-based semantic similarity, more manual tuning); see
          docs/implementation/01-narrative-cluster-discovery-surface.md Topic-Modeling Library.
    - option: Local web dashboard as the Technical Trader Solution's delivery interface (chosen
        with the human trader on 2026-07-08); see
        docs/architecture/01-technical-trader-solution.md Delivery Interface.
      alternatives_considered:
        - CLI plus notebook workflow; rejected as more manual for interactive chart inspection.
        - Plug into an existing third-party charting platform; rejected to avoid an external
          platform dependency for the first capability.
  repository_context:
    branch: sven_input
    base_commit: 98606b1622f93af7aec090557993d53c3143f62c
  current_implementation:
    architecture_document: docs/architecture/01-technical-trader-solution.md
    implementation_document: docs/implementation/01-narrative-cluster-discovery-surface.md
  guidelines:
    - All clustering is based strictly on shared or adjacent narrative/topic, without regard to
      quantitative pair bonds, sync signals, or existing theme membership (MB-013's reverse path).
    - Cluster tickers using topic modeling over extracted narrative findings, grouping by shared
      or overlapping dominant topics.
    - Narrative extraction and cluster surfacing refresh on the same nightly batch cadence as the
      existing bond-network computation.
    - Each cluster's storyline is displayed as a short topic label or summary, not full quoted
      excerpts or raw headlines.
    - Narrative clustering alone never creates a theme, per STR-011 - a theme is created only
      once the human trader inspects the clustered tickers' charts and confirms a sufficient
      portion shows a similar rhythm, price direction, or other discretionary chart
      characteristic.
    - The narrative extraction workflow should be extensible to other narrative sources as
      needed, supporting the cluster-driven human trader workflow.
  impact_analysis:
    methodology: >
      Exploits MB-013 (Narrative Formation) via STR-011 (Narrative-Led Cluster Discovery,
      Chart-Confirmed into a Theme): narrative convergence across a group of tickers can precede
      any sync-threshold crossing and constitutes an alternative theme-genesis path alongside the
      usual sync-first path; per-ticker narrative findings narrow investigation of what connects
      a candidate group independent of how it first surfaced. Chart-level human trader
      confirmation remains required before any theme is created; narrative strength alone never
      creates a theme.
    architecture: Encourages separation of concerns between nightly narrative extraction,
      topic-modeling clustering, and UI exposure.
    implementation: Topic-modeling-driven narrative grouping may expose tickers to cross-sectoral
      patterns invisible in traditional pair or theme-based analytics.
  development_cycle:
    steps:
      - Author docs/architecture/01-technical-trader-solution.md as the first architecture pass,
        covering component topology, solution_surface classification (coded_agent), and the
        fixed nightly pipeline decision, confirmed with the human trader.
      - Confirm the Technical Trader Solution's delivery interface (local web dashboard) with
        the human trader and record it in docs/architecture/01-technical-trader-solution.md.
      - Author docs/implementation/01-narrative-cluster-discovery-surface.md covering data
        contracts, local storage, the Streamlit-based Discovery Surface page, and CLI/SDK/MCP
        interface contracts.
      - Define development/backlog/active/001-narrative-cluster-discovery-surface-task-plan.yaml
        (6 ordered tasks, solution_surface coded_agent).
      - task_1_scaffold_package, owner python-creation-agent via the python-creation skill --
        scaffolded the technical_trader_solution uv-managed package (renamed from the
        placeholder sven-agentic-solution project name to match the product's canonical name
        and the already-decided tts CLI; a non-UI naming choice, documented here rather than
        escalated) with core/sdk/cli/mcp/logging.py, tests/{unit,sdk,cli,mcp,prompts,fixtures},
        demos/, and .gitlab-ci.yml, per the package-layout sub-skill's deterministic layout.
        Verified that `uv sync` resolves this workspace as a member of the parent
        agentic-trading-solutions uv workspace, `tts --help` runs via the installed console
        script, the FastMCP server module instantiates, and smoke tests pass (2 passed),
        confirming imports work and logging writes to stderr/file only, never stdout.
      - task_2_coded_agent_pipeline, owner coded-subagent-creation-agent via the
        coded-subagent-creation skill -- implemented the Narrative Extraction and Clustering
        Pipeline coded agent under src/technical_trader_solution/coded_agents/ (a
        self-contained base.py runtime -- LangGraph generate/evaluate flow, task-id keyed
        SQLite state persistence, 5-iteration refine cap, file logging -- plus
        narrative_clusters_agent/{models,storage,fetch,extract,cluster,label,trend,agent}.py
        implementing fetch, extract, cluster (BERTopic plus the Jaccard/cosine
        cluster-identity matching algorithm), label (one bounded Anthropic call per
        cluster), trend, and persist exactly per the implementation spec. Added
        tests/fixtures/narrative_clusters/documents.yaml and prior_run_clusters.yaml, and
        tests/unit/test_cluster_matching.py plus test_narrative_clusters_agent.py (marked
        live_endpoint). Verified end to end against the two-topic fixture with a real
        Anthropic label call, not merely tests passing -- the pipeline correctly split six
        tickers into two clusters matching the fixture's two planted topics, CEG/NVDA/VST
        labeled "AI datacenter power surge" and IONQ/QBTS/RGTI labeled "Quantum computing
        progress and milestones", with correct frequency_of_mention and company_breadth.
        All 7 suite tests pass.
      - task_3_invocation_surface_wiring, owner python-creation-agent via the python-creation
        skill -- generated SDK (technical_trader_solution/sdk/narrative_clusters.py), CLI
        (tts narrative run-nightly/show-clusters/show-trend, tts mcp serve), and MCP
        (narrative_run_nightly_pipeline/get_clusters/get_cluster_trend, registered in
        .mcp.json) invocation surfaces, all reusing the same core functions. Corrected the
        implementation spec's runtime-config mechanism from a section in
        agent-building-agent's own coded-agent-config.yaml (closed to its four framework
        agents) to a new tts-coded-agent-config.yaml, loaded by
        coded_agents/config.py. Resolved the task_2 review's deferred finding by adding
        ClusterView (excludes topic_embedding) as what every surface returns. Found and
        fixed a state-isolation bug during interface testing -- task-id state defaulted to
        a workspace-global path, so re-running with a shared task_id resumed an unrelated
        prior run's stale state; fixed by co-locating state with db_path. Added
        tests/{sdk,cli,mcp}/test_narrative_clusters_*.py (marked live_endpoint), each
        confirming topic_embedding never appears in a response. All 11 suite tests pass.
      - Integrate clustering logic into the Technical Trader Solution.
      - Implement narrative cluster surface with topic-label storyline, frequency, and breadth
        displays.
      - Enable navigation from cluster results to side-by-side chart inspection of cluster
        tickers.
      - task_4_discovery_surface_dashboard, owner python-creation-agent via the python-creation
        skill -- built the Streamlit + Plotly Discovery Surface page
        (src/technical_trader_solution/dashboard/discovery_surface.py) and the `tts dashboard
        serve` CLI entry point (cli/dashboard.py, launching `streamlit run` as a subprocess and
        forwarding --db-path/--log-file/--log-level after `--`). The page calls only
        sdk.narrative_clusters.get_clusters/get_cluster_trend for all cluster data (topic_label,
        member_tickers, frequency_of_mention, company_breadth, trend sparkline) and never
        queries NarrativeClustersStore/SQLite directly. An "Inspect charts" button reveals a
        side-by-side (one st.columns per member ticker) Plotly price-history view, sourced from
        a new core/market_data.py (fetch_historical_prices) calling FMP's historical-price-full
        endpoint directly -- the Discovery Surface's own data-access contract distinguishes this
        from the SDK-only cluster-data path, since FMP historical prices are not part of the
        narrative_clusters SDK's contract. No theme-creation control exists anywhere on the
        page, per STR-011. Added streamlit/plotly to pyproject.toml. Verified end to end with a
        browser-driven Playwright smoke test against a seeded fixture store (not merely a code
        read) -- cluster list renders topic_label/members/frequency/breadth/sparkline correctly;
        clicking "Inspect charts" reveals per-member-ticker columns; enumerated every on-page
        button and confirmed none reference theme creation; confirmed graceful (warning, not
        crash) degradation with FMP_API_KEY unset; zero browser console errors. Separately
        verified fetch_historical_prices's FMP-response parsing/oldest-first ordering against a
        mocked httpx transport. Smoke-test artifacts cleaned up (data/ is gitignored). The
        task_4 review checkpoint passed clean (task review-us-015-task4-dashboard-v4,
        2026-07-09) after resolving an agent-building-agent runtime defect encountered along
        the way -- see review_findings below for the full account. task_5_qa_validation has
        not been started.
      - task_5_qa_validation, owner qa-agent via the qa skill -- filled real test gaps before
        invoking qa-agent (which only executes pytest/yaml-check and narrates results, per
        its own runtime source; it does not author tests or demos itself) -- added
        tests/unit/test_extract.py and tests/unit/test_trend.py (extract.py and trend.py
        previously had no dedicated fixture-driven unit coverage, only indirect exercise via
        the live end-to-end test), tests/prompts/test_label_prompt.py (validate_label
        unit checks; a deterministic test that NarrativeClustersAgent.evaluate() rejects an
        invalid label and accepts a valid one, confirming the refinement-trigger contract;
        and a live_endpoint test of generate_topic_label's single-string-output contract
        against the real Anthropic API), and tests/unit/test_fetch_fmp_client.py (live
        FMPClient interface test, reusing documents.yaml's tickers, skips without
        FMP_API_KEY). Added demos/narrative_clusters_demo.py and ran it for real -- two
        multi-ticker clusters from the two-topic fixture via a genuine Anthropic label call
        ("AI datacenter power surge" for CEG/NVDA/VST; "Quantum Computing Breakthroughs"
        for IONQ/QBTS/RGTI, both frequency_of_mention=6/company_breadth=3), then launched `tts
        dashboard serve` against the demo store and drove it with a browser-driven
        Playwright script confirming both clusters render with a working "Inspect charts"
        chart-inspection link and no theme-creation control. Ran the full suite directly
        (`uv run pytest tests`) -- 28 passed, 1 skipped (the FMP-client test, FMP_API_KEY not
        configured in this environment) -- also confirmed no bare print() calls exist in
        the generated src/ tree, per the QA logging-compliance check.
        coded_agent_invoke(qa-agent) (task qa-us-015-task5-v1) then ran for real -- yaml-check
        on the story and task-plan files passed; its own internal run_pytest("tests") call
        reported fail, traced to the same class of environment defect as task_4's max_tokens
        bug -- agent-building-agent's own local .venv at this project's root (where its
        console-script shim's shebang points) is a separate, stale, partial environment
        that was never synced with this project's pytest/pyyaml dev-dependency group
        (only the parent uv workspace's shared venv has them, which `uv run pytest`
        correctly resolves to); confirmed by reproducing the identical
        "No module named pytest" error by invoking that exact interpreter directly.
        Per the human trader's direction (asked, since this is agent-building-agent's own
        environment, not story 001's), left that environment unmodified rather than
        patching it; qa-agent's raw pass_fail_result.pytest="fail" is recorded below as a
        known false negative, superseded by the directly-verified 28-passed/1-skipped
        result above. qa-agent set developer_approval_required=true for the prompt-flow
        and demo-flow evidence, per the QA approval-boundaries sub-skill; that developer
        approval is requested from the human trader separately, not self-granted.
        Developer approval granted 2026-07-09 for both the prompt-flow evidence (live
        label-node output, refinement-trigger behavior) and the demo-flow evidence
        (two-cluster demo run, dashboard rendering with a working chart-inspection link).
        One non-blocking cosmetic note accepted as a deferred item -- with only one trend
        snapshot per cluster so far, Plotly's auto-ranged x-axis renders sub-second tick
        labels instead of just the date; expected to read normally once multiple nightly
        runs accumulate real dates to span, not a data-correctness defect.
      - task_6_final_review, owner review-validation-agent via coded_agent_invoke -- final
        review gate across code, tests, demo, and documentation alignment (task
        review-us-015-task6-final-v1, 2026-07-09). Before invoking it, checked
        docs/architecture/01-technical-trader-solution.md and docs/implementation/
        01-narrative-cluster-discovery-surface.md against the shipped code directly --
        no drift found; both documents' status markers already read "generated" and
        match what is actually shipped, and docs/README.md's document-lookup index
        already references both. Passed clean (gate_result.state=pass, 7 findings, all
        informational) -- all five story success criteria confirmed demonstrably met by
        shipped code and test/demo evidence; SDK/CLI/MCP mandatory-generation rule
        confirmed satisfied; the two infrastructure caveats from task_4 (max_tokens fix)
        and task_5 (qa-agent's stale-.venv false negative) confirmed correctly classified
        as non-story defects, not blocking findings. No new blocking, important, or minor
        findings raised. Story moved from active to implemented below.
    review_findings:
      - severity: blocking
        finding: Tech-restricted term 'agentic'/'agent' appeared in functional success_criteria
          and guidelines
        resolution: Reworded to capability language; agent/non-agent choice moved to
          architecture_options.
      - severity: blocking
        finding: Bare 'trader' in guidelines
        resolution: Corrected to 'human trader'.
      - severity: blocking
        finding: success_criteria did not make theme-creation discretion explicit
        resolution: Added explicit success_criteria entry and guideline citing STR-011.
      - severity: important
        finding: Story did not cite MB-013/STR-011
        resolution: Added citation to impact_analysis.methodology and goal.
      - severity: info
        finding: Open questions on clustering definition, refresh cadence, and storyline
          granularity required human trader input
        resolution: >
          Human trader chose (1) topic modeling with topic overlap for clustering, (2) nightly
          batch cadence aligned with existing bond-network computation, (3) short topic label or
          summary for displayed storylines. Incorporated into success_criteria and guidelines;
          open_questions cleared.
      - severity: info
        finding: Architecture review gate (docs/architecture/01-technical-trader-solution.md,
          docs/README.md index update, story architecture_options/current_implementation)
        resolution: >
          Passed with deferred follow-up (task review-us-015-architecture-v1, 2026-07-08).
          Solution-surface classification coded_agent confirmed correctly justified via the
          two-part test; STR-011/MB-020 consistency confirmed; docs/README.md index update
          confirmed; vocabulary compliance confirmed. One non-blocking, deferred cosmetic
          note about the Component Topology diagram format did not require a change (the
          document already uses a fenced mermaid code block).
      - severity: important
        finding: Implementation-spec review gate found the cluster-identity persistence
          matching algorithm under-specified
        resolution: >
          Passed with deferred follow-up (task review-us-015-implementation-spec-v1,
          2026-07-08). Explicitly deferred the matching-algorithm design (overlap threshold,
          dominant-topic tie-breaking, new-cluster-creation criteria) to the task plan, to be
          resolved after topic-modeling library selection and before implementation execution.
          Also incorporated the non-deferrable label-node output-validation expectation, and
          two info/minor traceability clarifications (FMP historical-price cross-reference,
          topic-modeling library deferral rationale) directly into the implementation spec.
      - severity: blocking
        finding: Task-plan review gate found deferring the cluster-matching algorithm and
          topic-modeling library choice into task execution itself (rather than resolving
          them at the implementation-spec layer first) violated abstraction alignment; also
          found a contradiction between an MCP-surface exemption claim and generating MCP,
          plus several important/minor holes (dashboard data-access ambiguity, missing
          fixture paths, unclear parallelization model, undefined CLI name)
        resolution: >
          Blocked on first pass (task review-us-015-task-plan-v1, 2026-07-08). Resolved by
          updating docs/implementation/01-narrative-cluster-discovery-surface.md v0.3
          directly (not the task plan) with the concrete Jaccard/cosine cluster-matching
          algorithm, the BERTopic library choice with alternatives considered, the `tts` CLI
          name, the dashboard's SDK-only data-access contract, and fixture file paths. Task
          plan updated to implement (not choose) these spec-level decisions, corrected the
          MCP/packaging scope wording, and simplified the parallelization note. Both are
          non-UI implementation choices, decided without escalating to the human trader.
      - severity: info
        finding: Task-plan review gate re-run after the above resolution
        resolution: >
          Passed clean (task review-us-015-task-plan-v2, 2026-07-08): all three prior
          blocking findings confirmed resolved; solution_surface/top_level_runtime/
          subagent_topology/phase_owners/invocation_surfaces block confirmed complete and
          non-contradictory; ordered_tasks/prerequisite_artifacts confirmed a valid,
          cycle-free dependency order. One minor, deferrable note about fixture-count
          specificity accepted as-is. Task plan file:
          development/backlog/active/001-narrative-cluster-discovery-surface-task-plan.yaml.
      - severity: important
        finding: Task_2 review checkpoint found ClusterRecord.topic_embedding (an internal
          BERTopic vector used only by the cluster-identity matching algorithm) is returned
          in the pipeline's run output and would be exposed through any SDK/CLI/MCP/
          dashboard call that returns ClusterRecord objects as-is
        resolution: >
          Passed with deferred follow-up (task review-us-015-task2-coded-agent-v1,
          2026-07-08): correctly scoped as a task_3 invocation-surface-wiring decision, not
          a task_2 defect. Deferred into task_3's scope below rather than a new story,
          since task_3 (owned by the same story) is the natural place to decide whether
          get_clusters/CLI/MCP/dashboard responses exclude topic_embedding.
      - severity: info
        finding: Task_3 review checkpoint (SDK/CLI/MCP wiring, coded-agent-config.yaml
          correction, state-isolation bugfix)
        resolution: >
          Passed clean (task review-us-015-task3-invocation-surfaces-v2, 2026-07-09).
          ClusterView confirmed to fully resolve the task_2 deferred finding across all
          three surfaces; the tts-coded-agent-config.yaml correction confirmed justified
          and does not leave the agent's model/prompt unconfigurable; the state-isolation
          bugfix confirmed sound; CLI/SDK/MCP confirmed aligned; local .mcp.json
          registration confirmed consistent with local tooling, not distributable
          packaging. First review-agent call (v1) failed after 5 iterations with empty
          model output when asked to read_docs four files; resolved by retrying (v2) with
          the same summary inlined and no read_docs instruction, not by changing scope.
      - severity: info
        finding: Task_4 review checkpoint (Discovery Surface dashboard page and `tts dashboard
          serve` CLI entry point) against the story's success criteria (no automated theme
          creation, STR-011 compliance, SDK-only data access)
        resolution: >
          Passed clean (task review-us-015-task4-dashboard-v4, 2026-07-09): SDK-only
          cluster-data access confirmed (discovery_surface.py calls only
          sdk.narrative_clusters.get_clusters/get_cluster_trend, never
          NarrativeClustersStore/SQLite); STR-011 compliance confirmed (no theme-creation
          control anywhere on the page, verified by enumerating every on-page button);
          member-ticker price charts confirmed sourced from FMP via the new
          core/market_data.py, kept independent of the narrative_clusters agent's own
          FMPClient; Streamlit subprocess launch convention, graceful FMP_API_KEY-unset
          degradation, and CLI registration all confirmed sound. All twelve findings
          informational; no blocking, important, or minor issues. Attempts v1-v3
          (2026-07-09) failed before reaching this content: v1-v2 (via the
          coded_agent_invoke MCP tool) and the CLI-path retries all hit an
          agent-building-agent runtime defect ("Streaming is required for operations that
          may take longer than 10 minutes") caused by review-validation-agent's
          max_tokens=65536 on claude-sonnet-4-5 in agent-building-agent's own
          coded-agent-config.yaml exceeding the Anthropic SDK's non-streaming timeout
          budget; resolved by lowering max_tokens to 16000 (per the human trader's
          direction) -- a fix to agent-building-agent's own framework config, not a
          story-001-owned file. v3 (after the max_tokens fix) then failed for an unrelated
          reason: the input payload didn't match review-validation-agent's actual expected
          input keys (review_target/content_excerpt/active_story/answers), so it correctly
          found "no reviewable content provided" and returned gate_result.state=blocked;
          v4 resubmitted with the correct input shape, embedding the actual story
          success_criteria/guidelines and the three new source files' full contents rather
          than a prose summary, and passed.
      - severity: info
        finding: Task_6 final review gate (code, tests, demo, and documentation alignment)
          before the story moves from active to implemented
        resolution: >
          Passed clean (task review-us-015-task6-final-v1, 2026-07-09): all five story
          success criteria confirmed demonstrably met by shipped code and test/demo
          evidence; SDK/CLI/MCP mandatory-generation rule confirmed satisfied (all three
          surfaces generated, reusing the same core functions, backed by dedicated
          tests); code/tests/demo/documentation alignment confirmed with no drift after
          an explicit direct check of docs/architecture/01-technical-trader-solution.md
          and docs/implementation/01-narrative-cluster-discovery-surface.md against the
          shipped code; the task_4 max_tokens fix and task_5 qa-agent stale-.venv false
          negative both confirmed correctly classified as non-story infrastructure
          caveats, not blocking findings. Seven findings, all informational; no
          blocking, important, or minor issues. Story satisfies all task-plan
          completion_conditions and moves to implemented.
    deferred_items:
      - item: Discovery Surface trend sparkline's x-axis renders sub-second tick labels
          (for example "23:59:59.999") when a cluster has only one trend snapshot, since
          Plotly auto-ranges the axis around a single timestamp.
        reason: Cosmetic only, not a data-correctness defect; accepted as-is during task_5
          developer approval on 2026-07-09. Expected to read as normal dates once multiple
          nightly runs accumulate more than one snapshot per cluster to span.
  open_questions: []
